# Figra v2.2.0

BibTeX 工作台升级到 v2.2：

- 默认启用去重和格式化，替换旧的四任务选择器。
- 新增重复项审阅主流程，可预览重复组、候选条目、保留项和将移除项。
- 支持 DOI、arXiv ID、标题、引用键和标题的强重复识别。
- Key 冲突与疑似重复默认保留待审，避免误删。
- 支持按重复组改为保留其他候选、全部保留或恢复默认。
- 左侧源文件优先级决定默认保留顺序，添加文件后仍可继续拖入。
- 详情区新增字段摘要、重复理由和 BibTeX 预览。
